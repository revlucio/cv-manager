@model CvManager.Model.Cv

@for (var i = 0; i < Model.Conferences.Count; i++)
{
    <li>
        @Html.HiddenFor(m => m.Conferences[i].Id)
        @Html.EditorFor(m => m.Conferences[i].Name)
        <input type="button" value="remove" class="removeA" data-val="@i">
    </li>
}
