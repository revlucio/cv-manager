@model CvManager.Model.Cv

@for (var i = 0; i < Model.Achievements.Count; i++)
{
    <li>
        @Html.EditorFor(m => m.Achievements[i].Name)
        <input type="button" value="remove" class="removeA" data-val="@i">
    </li>
}
